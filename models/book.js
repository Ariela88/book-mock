class Book {
    constructor(title, author, dop, genre, cover) {
      this.title = title;
      this.author = author;
      this.dop = dop;
      this.genre = genre;
      this.cover = cover;
    }
  }